https://docs.python.org/3/library/turtle.html

''' 

screen:
window = turtle.Screen()
window.bgcolor(color)
window.exitonclick() # wait for input to exit window screen


obj = turtle.Turtle() 

methods: 
obj.forward(length)
obj.back(length)
obj.left(degree)
obj.right(degree)
obj.speed(int) (1 to 10)

obj.pensize(int)
obj.penup() or obj.up()
obj.pendown() or obj.down()

attribute:
obj.Shape('') {arrow, blank, circle, classic, square, triangle, turtle}
obj.color(color)

obj.heading()
obj.position()
obj.width()

'''

